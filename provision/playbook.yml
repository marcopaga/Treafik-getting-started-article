---
- hosts: all
  become: yes
  gather_facts: true
  serial: 2

  pre_tasks:
    - name: Update apt cache if needed.
      apt: update_cache=yes cache_valid_time=3600

  tasks:
    - include: setup.yml
    - include: docker.yml